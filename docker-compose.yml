services:
  graphify-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: graphify-local-server
    restart: unless-stopped
    ports:
      - "${PORT:-28848}:28848"
    environment:
      - PORT=28848
      - GRAPHIFY_DATA_DIR=/app/data
      - GRAPHIFY_STATIC_DIR=/app/static
      - GRAPHIFY_VIZ_NODE_LIMIT=50000
    volumes:
      - ./data:/app/data
      - ./server:/app/server
      - ./static:/app/static
      - ${HOST_WORKSPACE_DIR:-./workspaces}:/app/workspaces
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:28848/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
